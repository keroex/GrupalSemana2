<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.utec.grupalsemana2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GrupalSemana2"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.login.Login"
            android:exported="true"
            android:label="@string/title_activity_login" />

        <service
            android:name=".servicios.ServicioMostrarLog"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".presentacion.ListarActividadesDeCampo"
            android:exported="true" />
        <activity
            android:name=".presentacion.AltaActividadDeCampo"
            android:exported="true" />
        <activity
            android:name=".presentacion.MostraActividadDeCampo"
            android:exported="true" />
    </application>

</manifest>